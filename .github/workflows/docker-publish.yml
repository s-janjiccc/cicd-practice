name: Build & Push Docker image

# starts on push on main branch
on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: docker build -t janjicdev/learn-docker:latest .

      - name: Push to Docker Hub
        run: docker push janjicdev/learn-docker:latest

      # example of auto deploy to azure (azure web app for containers)
      #- name: Deploy to Azure web app
      #  uses: azure/webapps-deploy@v2
      #  with:
      #    app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
      #    publish-profile: |
      #      {
      #        "publishingUserName": "${{ secrets.AZURE_USERNAME }}",
      #        "publishingPassword": "${{ secrets.AZURE_PASSWORD }}"
      #      }
      #    images: janjicdev/learn-docker:latest
